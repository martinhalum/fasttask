/**
*
* {{ properCase name }} Test
*
*/


import React from 'react';
import {render} from '@testing-library/react-native';

import {{ properCase name }} from '../{{ properCase name }}';

test('{{ properCase name }}', () => {
  const tree = renderer
    .create(
      <{{ properCase name }} />
    )
    .toJSON();
  expect(tree).toMatchSnapshot();
});
