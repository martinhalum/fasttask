/**
 *
 * NavButtons Styles
 *
 */

import {StyleSheet} from 'react-native';

const NavButtonsStyles = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});

export default NavButtonsStyles;
